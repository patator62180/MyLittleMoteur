{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Debug",
      "preLaunchTask": "build-debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bazel-bin/bin.exe"
    }
  ]
}
